import 'package:flutter/material.dart';

class SyncErrorsScreen extends StatelessWidget {
  const SyncErrorsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Sync Errors')),
      body: const Center(child: Text('Sync Errors')),
    );
  }
}
