import 'package:flutter/material.dart';

class CondominiumSyncScreen extends StatelessWidget {
  const CondominiumSyncScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Condominium Sync')),
      body: const Center(child: Text('Condominium Sync')),
    );
  }
}
