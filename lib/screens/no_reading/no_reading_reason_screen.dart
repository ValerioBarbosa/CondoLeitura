import 'package:flutter/material.dart';

class NoReadingReasonScreen extends StatelessWidget {
  const NoReadingReasonScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('No Reading Reason')),
      body: const Center(child: Text('No Reading Reason')),
    );
  }
}
