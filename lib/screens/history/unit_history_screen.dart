import 'package:flutter/material.dart';

class UnitHistoryScreen extends StatelessWidget {
  const UnitHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Unit History')),
      body: const Center(child: Text('Unit History')),
    );
  }
}
