import 'package:flutter/material.dart';

class ReadingDetailsScreen extends StatelessWidget {
  const ReadingDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Reading Details')),
      body: const Center(child: Text('Reading Details')),
    );
  }
}
