import 'package:flutter/material.dart';

class CondominiumImportScreen extends StatelessWidget {
  const CondominiumImportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Condominium Import')),
      body: const Center(child: Text('Condominium Import')),
    );
  }
}
