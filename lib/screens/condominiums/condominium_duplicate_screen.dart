import 'package:flutter/material.dart';

class CondominiumDuplicateScreen extends StatelessWidget {
  const CondominiumDuplicateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Condominium Duplicate')),
      body: const Center(child: Text('Condominium Duplicate')),
    );
  }
}
