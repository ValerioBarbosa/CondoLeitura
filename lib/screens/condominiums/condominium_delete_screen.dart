import 'package:flutter/material.dart';

class CondominiumDeleteScreen extends StatelessWidget {
  const CondominiumDeleteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Condominium Delete')),
      body: const Center(child: Text('Condominium Delete')),
    );
  }
}
