import 'package:flutter/material.dart';

class CondominiumArchivedScreen extends StatelessWidget {
  const CondominiumArchivedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Condominium Archived')),
      body: const Center(child: Text('Condominium Archived')),
    );
  }
}
