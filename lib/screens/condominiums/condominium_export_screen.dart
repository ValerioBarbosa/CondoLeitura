import 'package:flutter/material.dart';

class CondominiumExportScreen extends StatelessWidget {
  const CondominiumExportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Condominium Export')),
      body: const Center(child: Text('Condominium Export')),
    );
  }
}
