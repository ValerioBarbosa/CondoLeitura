import 'package:flutter/material.dart';

class CondominiumsScreen extends StatelessWidget {
  const CondominiumsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Condominiums')),
    );
  }
}
