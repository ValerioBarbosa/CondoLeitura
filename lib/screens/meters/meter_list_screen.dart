import 'package:flutter/material.dart';

class MeterListScreen extends StatelessWidget {
  const MeterListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Meter List')),
      body: const Center(child: Text('Meter List')),
    );
  }
}
