import 'package:flutter/material.dart';

class MeterFormScreen extends StatelessWidget {
  const MeterFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Meter Form')),
      body: const Center(child: Text('Meter Form')),
    );
  }
}
