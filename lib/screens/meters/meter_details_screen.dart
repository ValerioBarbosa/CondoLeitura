import 'package:flutter/material.dart';

class MeterDetailsScreen extends StatelessWidget {
  const MeterDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Meter Details')),
      body: const Center(child: Text('Meter Details')),
    );
  }
}
