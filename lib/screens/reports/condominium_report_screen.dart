import 'package:flutter/material.dart';

class CondominiumReportScreen extends StatelessWidget {
  const CondominiumReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Condominium Report')),
      body: const Center(child: Text('Condominium Report')),
    );
  }
}
